<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- Mybatis-mapper -->

<mapper namespace="com.activity.mybatis-mappers.ContentMapper">

	<sql id="contentTable">
		content_no,
		content_name,
		content_info,
		content_price,
		content_capacity,
		content_yn,
		admin_email		
	</sql>
	
	
<!-- ====삽입 쿼리====  -->





<!-- ====조회 쿼리====  -->

<!-- 컨텐츠 리스트 조회 (테스트용,임시) -->
<select id="getContentList" parameterType="ContentDTO" resultType="ContentDTO">
	SELECT
		<include refid="contentTable"></include>
	FROM 
		CONTENT
</select>

<!-- 컨텐츠 한건 조회(컨텐츠 번호 10000001로 테스트 조회)  -->
<select id="getContentInfo" parameterType="ContentDTO" resultType="ContentDTO">
	SELECT 
		<include refid="contentTable"></include>
	FROM
		CONTENT
	WHERE
		content_no = 10000001;
</select>

</mapper>